import { u as useNssStore } from "./utils-BIiJ-s-U.js";
import { t as Button } from "./button-HEpMk59d.js";
import { i as CardTitle, n as CardContent, r as CardHeader, t as Card } from "./card-2ADCK2Ma.js";
import { useRef, useState } from "react";
import { jsx, jsxs } from "react/jsx-runtime";
import { DatabaseBackup, Download, RotateCcw, Trash2, UploadCloud } from "lucide-react";
import { toast } from "sonner";
//#region src/routes/po/backup.tsx?tsr-split=component
function BackupPage() {
	const state = useNssStore();
	const fileRef = useRef(null);
	const [busy, setBusy] = useState(false);
	const recycleBin = state.recycleBin ?? [];
	const exportBackup = () => {
		const payload = {
			version: 3,
			exportedAt: (/* @__PURE__ */ new Date()).toISOString(),
			source: "NSP",
			state: {
				volunteers: state.volunteers,
				events: state.events,
				attendance: state.attendance,
				eventRsvps: state.eventRsvps,
				notices: state.notices,
				gallery: state.gallery,
				certificates: state.certificates,
				pressReports: state.pressReports,
				logs: state.logs,
				settings: state.settings,
				visitors: state.visitors,
				recycleBin: state.recycleBin
			}
		};
		const blob = new Blob([JSON.stringify(payload, null, 2)], { type: "application/json" });
		const url = URL.createObjectURL(blob);
		const a = document.createElement("a");
		a.href = url;
		a.download = `NSP-backup-${(/* @__PURE__ */ new Date()).toISOString().slice(0, 10)}.json`;
		a.click();
		URL.revokeObjectURL(url);
		toast.success("Complete NSP backup downloaded.");
	};
	async function importBackup(file) {
		if (!file) return;
		setBusy(true);
		try {
			const parsed = JSON.parse(await file.text());
			const data = parsed && typeof parsed === "object" && "state" in parsed ? parsed.state : parsed;
			if (!Array.isArray(data.volunteers) || !Array.isArray(data.events)) throw new Error("Invalid NSP backup");
			state.importBackup(data);
			toast.success("Backup restored on this device.");
		} catch (e) {
			toast.error(e instanceof Error ? e.message : "Could not restore backup.");
		} finally {
			setBusy(false);
		}
	}
	return /* @__PURE__ */ jsxs("div", {
		className: "space-y-6",
		children: [
			/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("h2", {
				className: "font-display text-2xl font-bold",
				children: "Backup & Restore"
			}), /* @__PURE__ */ jsx("p", {
				className: "text-sm text-muted-foreground",
				children: "Portable full backup for NSS records, including RSVP, press reports and Recycle Bin."
			})] }),
			/* @__PURE__ */ jsxs("div", {
				className: "grid gap-4 md:grid-cols-2",
				children: [/* @__PURE__ */ jsxs(Card, { children: [/* @__PURE__ */ jsx(CardHeader, { children: /* @__PURE__ */ jsxs(CardTitle, {
					className: "flex items-center gap-2",
					children: [/* @__PURE__ */ jsx(DatabaseBackup, { className: "size-5" }), " Create full backup"]
				}) }), /* @__PURE__ */ jsxs(CardContent, { children: [/* @__PURE__ */ jsx("p", {
					className: "text-sm text-muted-foreground",
					children: "Exports students, events, RSVP, attendance, gallery, certificates, press reports, settings and audit logs."
				}), /* @__PURE__ */ jsxs(Button, {
					className: "mt-4",
					onClick: exportBackup,
					children: [/* @__PURE__ */ jsx(Download, {}), " Download NSP Backup"]
				})] })] }), /* @__PURE__ */ jsxs(Card, { children: [/* @__PURE__ */ jsx(CardHeader, { children: /* @__PURE__ */ jsxs(CardTitle, {
					className: "flex items-center gap-2",
					children: [/* @__PURE__ */ jsx(UploadCloud, { className: "size-5" }), " Restore backup"]
				}) }), /* @__PURE__ */ jsxs(CardContent, { children: [
					/* @__PURE__ */ jsx("p", {
						className: "text-sm text-muted-foreground",
						children: "Restore a previously exported NSP JSON file to this browser."
					}),
					/* @__PURE__ */ jsx("input", {
						ref: fileRef,
						type: "file",
						accept: "application/json,.json",
						className: "hidden",
						onChange: (e) => void importBackup(e.target.files?.[0])
					}),
					/* @__PURE__ */ jsx(Button, {
						className: "mt-4",
						variant: "outline",
						disabled: busy,
						onClick: () => fileRef.current?.click(),
						children: busy ? "Restoring…" : "Choose Backup File"
					})
				] })] })]
			}),
			/* @__PURE__ */ jsxs(Card, { children: [/* @__PURE__ */ jsx(CardHeader, { children: /* @__PURE__ */ jsxs(CardTitle, {
				className: "flex items-center gap-2",
				children: [
					/* @__PURE__ */ jsx(Trash2, { className: "size-5" }),
					" Recycle Bin ",
					/* @__PURE__ */ jsxs("span", {
						className: "text-sm font-normal text-muted-foreground",
						children: [
							"(",
							recycleBin.length,
							")"
						]
					})
				]
			}) }), /* @__PURE__ */ jsxs(CardContent, {
				className: "space-y-3",
				children: [
					/* @__PURE__ */ jsx("p", {
						className: "text-sm text-muted-foreground",
						children: "Deleted volunteers and events are retained here instead of being permanently lost."
					}),
					recycleBin.length === 0 ? /* @__PURE__ */ jsx("p", {
						className: "rounded-xl bg-muted p-4 text-sm text-muted-foreground",
						children: "Recycle Bin is empty."
					}) : /* @__PURE__ */ jsx("div", {
						className: "space-y-2",
						children: recycleBin.map((item) => /* @__PURE__ */ jsxs("div", {
							className: "flex flex-wrap items-center justify-between gap-3 rounded-xl border border-border p-3",
							children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("p", {
								className: "font-medium",
								children: item.label
							}), /* @__PURE__ */ jsxs("p", {
								className: "text-xs text-muted-foreground",
								children: [
									item.kind,
									" · ",
									new Date(item.deletedAt).toLocaleString()
								]
							})] }), /* @__PURE__ */ jsxs("div", {
								className: "flex gap-2",
								children: [/* @__PURE__ */ jsxs(Button, {
									size: "sm",
									variant: "outline",
									onClick: () => {
										state.restoreDeleted(item.id);
										toast.success("Restored from Recycle Bin.");
									},
									children: [/* @__PURE__ */ jsx(RotateCcw, {}), " Restore"]
								}), /* @__PURE__ */ jsxs(Button, {
									size: "sm",
									variant: "danger",
									onClick: () => {
										if (confirm("Permanently delete this item?")) state.permanentlyDelete(item.id);
									},
									children: [/* @__PURE__ */ jsx(Trash2, {}), " Delete"]
								})]
							})]
						}, item.id))
					}),
					recycleBin.length ? /* @__PURE__ */ jsx(Button, {
						variant: "outline",
						onClick: () => {
							if (confirm("Permanently empty the Recycle Bin?")) state.emptyRecycleBin();
						},
						children: "Empty Recycle Bin"
					}) : null
				]
			})] })
		]
	});
}
//#endregion
export { BackupPage as component };
