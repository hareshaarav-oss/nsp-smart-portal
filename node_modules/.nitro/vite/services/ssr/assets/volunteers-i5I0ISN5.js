import { P as parseDob, S as academicYear, a as bestVolunteer, d as volunteerHours, f as volunteerRecord, g as compressPassport, i as attendanceOf, k as formatLongDate, m as blobToDataUrl, n as activeVolunteers, u as useNssStore } from "./utils-BIiJ-s-U.js";
import { t as Button } from "./button-HEpMk59d.js";
import { n as CardContent, t as Card } from "./card-2ADCK2Ma.js";
import { t as OfficialLetterhead } from "./official-letterhead-0CBv-cY2.js";
import { t as Badge } from "./badge-BMh2Hxac.js";
import { t as Input } from "./input-CRT4sBU_.js";
import { t as Label } from "./label-Bl0lQyM7.js";
import { u as downloadVolunteerExcel } from "./reports-0LItQuD8.js";
import { t as qrSvg } from "./qr-r2hriz89.js";
import { i as openCertificateDocument, n as htmlForCertificate } from "./certificates-BpqtJ9x9.js";
import { t as openIdCard } from "./id-card-BhQsWlP5.js";
import { useMemo, useState } from "react";
import { jsx, jsxs } from "react/jsx-runtime";
import { CreditCard, QrCode, Star } from "lucide-react";
import { toast } from "sonner";
//#region src/routes/po/volunteers.tsx?tsr-split=component
function VolunteersPage() {
	const state = useNssStore();
	const [q, setQ] = useState("");
	const [course, setCourse] = useState("");
	const [semester, setSemester] = useState("");
	const [selected, setSelected] = useState([]);
	const [editing, setEditing] = useState(null);
	const [recordId, setRecordId] = useState(null);
	const best = bestVolunteer(state);
	const rows = useMemo(() => {
		const needle = q.toLowerCase();
		return activeVolunteers(state).filter((v) => {
			const hit = !needle || v.fullName.toLowerCase().includes(needle) || v.volunteerId.toLowerCase().includes(needle) || v.enrollment.toLowerCase().includes(needle) || v.mobile.includes(needle);
			const c = !course || v.course === course;
			const s = !semester || v.semester === semester;
			return hit && c && s;
		});
	}, [
		state,
		q,
		course,
		semester
	]);
	function toggle(id) {
		setSelected((cur) => cur.includes(id) ? cur.filter((x) => x !== id) : [...cur, id]);
	}
	function promote() {
		if (!selected.length) {
			toast.error("Select at least one volunteer.");
			return;
		}
		const n = state.promoteVolunteers(selected);
		toast.success(`Promoted ${n} volunteer(s) to the next semester.`);
		setSelected([]);
	}
	function removeSelected() {
		if (!selected.length) {
			toast.error("Select at least one volunteer.");
			return;
		}
		if (!confirm(`Delete ${selected.length} volunteer(s) permanently?`)) return;
		state.deleteVolunteers(selected);
		toast.success("Removed from the roll.");
		setSelected([]);
	}
	return /* @__PURE__ */ jsxs("div", {
		className: "space-y-4",
		children: [
			/* @__PURE__ */ jsx(OfficialLetterhead, {
				title: "Volunteer roll",
				compact: true
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "flex flex-wrap items-end justify-between gap-3",
				children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("h2", {
					className: "font-display text-xl font-semibold",
					children: "Volunteers"
				}), /* @__PURE__ */ jsxs("p", {
					className: "text-sm text-muted-foreground",
					children: [rows.length, " on the roll · A–Z · first name in CAPITAL · tick to promote or delete"]
				})] }), /* @__PURE__ */ jsx(Button, {
					onClick: () => downloadVolunteerExcel(state),
					children: "Download Excel"
				})]
			}),
			best ? /* @__PURE__ */ jsx(Card, {
				className: "border-saffron/40 bg-saffron/5",
				children: /* @__PURE__ */ jsxs(CardContent, {
					className: "flex flex-wrap items-center justify-between gap-3 pt-5",
					children: [/* @__PURE__ */ jsxs("p", {
						className: "text-sm",
						children: [
							/* @__PURE__ */ jsx(Star, { className: "mr-2 inline size-4 text-saffron" }),
							"Best volunteer: ",
							/* @__PURE__ */ jsx("strong", { children: best.fullName }),
							" · ",
							best.volunteerId,
							" ·",
							" ",
							volunteerHours(state, best.id),
							" hrs"
						]
					}), /* @__PURE__ */ jsx(Button, {
						size: "sm",
						variant: "outline",
						onClick: () => setRecordId(best.id),
						children: "Smart record"
					})]
				})
			}) : null,
			/* @__PURE__ */ jsxs("div", {
				className: "flex flex-wrap gap-2 rounded-lg border border-border bg-muted/40 p-3",
				children: [
					/* @__PURE__ */ jsx(Button, {
						size: "sm",
						onClick: promote,
						disabled: !selected.length,
						children: "Promote selected (Sem++)"
					}),
					/* @__PURE__ */ jsx(Button, {
						size: "sm",
						variant: "danger",
						onClick: removeSelected,
						disabled: !selected.length,
						children: "Delete selected"
					}),
					/* @__PURE__ */ jsxs("span", {
						className: "self-center text-xs text-muted-foreground",
						children: [selected.length, " selected"]
					})
				]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "grid gap-2 sm:grid-cols-3",
				children: [
					/* @__PURE__ */ jsx(Input, {
						placeholder: "Search name, ID, mobile",
						value: q,
						onChange: (e) => setQ(e.target.value)
					}),
					/* @__PURE__ */ jsxs("select", {
						className: "h-10 rounded-md border border-border bg-card px-3 text-sm",
						value: course,
						onChange: (e) => setCourse(e.target.value),
						children: [
							/* @__PURE__ */ jsx("option", {
								value: "",
								children: "All courses"
							}),
							/* @__PURE__ */ jsx("option", { children: "B.A." }),
							/* @__PURE__ */ jsx("option", { children: "B.Com." }),
							/* @__PURE__ */ jsx("option", { children: "B.Sc." })
						]
					}),
					/* @__PURE__ */ jsxs("select", {
						className: "h-10 rounded-md border border-border bg-card px-3 text-sm",
						value: semester,
						onChange: (e) => setSemester(e.target.value),
						children: [/* @__PURE__ */ jsx("option", {
							value: "",
							children: "All semesters"
						}), [
							"1",
							"2",
							"3",
							"4",
							"5",
							"6"
						].map((s) => /* @__PURE__ */ jsxs("option", {
							value: s,
							children: ["Semester ", s]
						}, s))]
					})
				]
			}),
			editing ? /* @__PURE__ */ jsx(EditVolunteer, {
				volunteer: editing,
				onClose: () => setEditing(null)
			}) : null,
			recordId ? /* @__PURE__ */ jsx(SmartRecord, {
				volunteerId: recordId,
				onClose: () => setRecordId(null)
			}) : null,
			/* @__PURE__ */ jsx(Card, { children: /* @__PURE__ */ jsx(CardContent, {
				className: "overflow-x-auto p-0",
				children: /* @__PURE__ */ jsxs("table", {
					className: "w-full min-w-[960px] text-left text-sm",
					children: [/* @__PURE__ */ jsx("thead", {
						className: "bg-primary text-primary-foreground",
						children: /* @__PURE__ */ jsx("tr", { children: [
							"",
							"#",
							"ID",
							"Name",
							"DOB",
							"Mobile",
							"Course",
							"Role",
							"Hours",
							"Actions"
						].map((h) => /* @__PURE__ */ jsx("th", {
							className: "px-3 py-2 font-medium",
							children: h === "" ? /* @__PURE__ */ jsx("input", {
								type: "checkbox",
								checked: rows.length > 0 && rows.every((v) => selected.includes(v.id)),
								onChange: (e) => setSelected(e.target.checked ? rows.map((v) => v.id) : [])
							}) : h
						}, h || "sel")) })
					}), /* @__PURE__ */ jsx("tbody", { children: rows.map((v, i) => /* @__PURE__ */ jsxs("tr", {
						className: "border-t border-border",
						children: [
							/* @__PURE__ */ jsx("td", {
								className: "px-3 py-2",
								children: /* @__PURE__ */ jsx("input", {
									type: "checkbox",
									checked: selected.includes(v.id),
									onChange: () => toggle(v.id)
								})
							}),
							/* @__PURE__ */ jsx("td", {
								className: "px-3 py-2 tabular-nums",
								children: i + 1
							}),
							/* @__PURE__ */ jsx("td", {
								className: "px-3 py-2 font-medium",
								children: v.volunteerId
							}),
							/* @__PURE__ */ jsx("td", {
								className: "px-3 py-2",
								children: /* @__PURE__ */ jsxs("div", {
									className: "flex items-center gap-2",
									children: [
										v.photoUrl ? /* @__PURE__ */ jsx("img", {
											src: v.photoUrl,
											alt: "",
											className: "size-8 rounded object-cover"
										}) : null,
										v.fullName,
										best?.id === v.id ? /* @__PURE__ */ jsx(Star, { className: "size-3.5 text-saffron" }) : null
									]
								})
							}),
							/* @__PURE__ */ jsx("td", {
								className: "px-3 py-2 text-xs",
								children: v.dob ? formatLongDate(parseDob(v.dob)) : "—"
							}),
							/* @__PURE__ */ jsx("td", {
								className: "px-3 py-2 tabular-nums",
								children: v.mobile
							}),
							/* @__PURE__ */ jsxs("td", {
								className: "px-3 py-2",
								children: [
									v.course,
									" ",
									v.semester
								]
							}),
							/* @__PURE__ */ jsx("td", {
								className: "px-3 py-2",
								children: /* @__PURE__ */ jsxs("select", {
									className: "rounded-md border border-border bg-card px-2 py-1 text-xs",
									value: v.nssRole,
									onChange: (e) => state.updateVolunteer(v.id, { nssRole: e.target.value }),
									children: [
										/* @__PURE__ */ jsx("option", { children: "Volunteer" }),
										/* @__PURE__ */ jsx("option", { children: "Leader" }),
										/* @__PURE__ */ jsx("option", { children: "Group Leader" }),
										/* @__PURE__ */ jsx("option", { children: "Discipline Head" }),
										/* @__PURE__ */ jsx("option", { children: "Camp Coordinator" })
									]
								})
							}),
							/* @__PURE__ */ jsx("td", {
								className: "px-3 py-2 tabular-nums",
								children: volunteerHours(state, v.id)
							}),
							/* @__PURE__ */ jsx("td", {
								className: "px-3 py-2",
								children: /* @__PURE__ */ jsxs("div", {
									className: "flex flex-wrap gap-1",
									children: [
										/* @__PURE__ */ jsx(Button, {
											size: "sm",
											variant: "outline",
											onClick: () => setRecordId(v.id),
											children: "Record"
										}),
										/* @__PURE__ */ jsx(Button, {
											size: "sm",
											variant: "outline",
											onClick: () => setEditing(v),
											children: "Edit"
										}),
										/* @__PURE__ */ jsxs(Button, {
											size: "sm",
											variant: "outline",
											onClick: () => void openIdCard(v, state.settings),
											children: [/* @__PURE__ */ jsx(CreditCard, {}), "ID"]
										}),
										/* @__PURE__ */ jsx(Button, {
											size: "sm",
											variant: "outline",
											onClick: () => {
												state.moveToAlumni(v.id, `Moved ${academicYear()}`);
												toast.success(`${v.fullName} sent to Alumni file`);
											},
											children: "Alumni"
										}),
										/* @__PURE__ */ jsx(Button, {
											size: "sm",
											variant: "ghost",
											onClick: () => {
												if (confirm(`Delete ${v.fullName}?`)) {
													state.deleteVolunteer(v.id);
													toast.success("Removed");
												}
											},
											children: "Delete"
										})
									]
								})
							})
						]
					}, v.id)) })]
				})
			}) })
		]
	});
}
function SmartRecord({ volunteerId, onClose }) {
	const state = useNssStore();
	const [qrMarkup, setQrMarkup] = useState("");
	const [qrOpen, setQrOpen] = useState(false);
	const rec = volunteerRecord(state, volunteerId);
	const v = rec.volunteer;
	const events = [...state.events].sort((a, b) => b.date.localeCompare(a.date));
	if (!v) return null;
	async function showVolunteerQr() {
		const store = useNssStore.getState();
		const current = store.volunteers.find((row) => row.id === volunteerId);
		if (!current) return;
		const token = current.qrToken || `nss-vol-${current.id}-${Date.now()}-${Math.random().toString(36).slice(2, 10)}`;
		if (!current.qrToken) store.updateVolunteer(current.id, {
			qrToken: token,
			qrGeneratedAt: (/* @__PURE__ */ new Date()).toISOString()
		});
		try {
			const svg = await qrSvg(JSON.stringify({
				type: "NSS_VOLUNTEER_ID",
				volunteerId: current.id,
				token
			}), 260);
			setQrMarkup(svg);
			setQrOpen(true);
		} catch {
			toast.error("Unable to generate volunteer QR.");
		}
	}
	function printVolunteerQr() {
		if (!qrMarkup) return;
		const printVolunteer = useNssStore.getState().volunteers.find((row) => row.id === volunteerId);
		if (!printVolunteer) {
			toast.error("Volunteer record not found.");
			return;
		}
		const popup = window.open("", "_blank", "width=600,height=720");
		if (!popup) {
			toast.error("Please allow pop-ups to print the QR.");
			return;
		}
		popup.document.write(`
      <!doctype html>
      <html>
        <head>
          <title>NSS Volunteer QR</title>
          <style>
            body{font-family:Arial,sans-serif;text-align:center;padding:30px}
            svg{max-width:300px;margin:20px auto}
            @media print{button{display:none}}
          </style>
        </head>
        <body>
          <h2>${escapeHtml(printVolunteer.fullName)}</h2>
          <p>${escapeHtml(printVolunteer.volunteerId)} · ${escapeHtml(printVolunteer.unit)}</p>
          ${qrMarkup}
          <button onclick="window.print()">Print QR</button>
        </body>
      </html>
    `);
		popup.document.close();
		popup.focus();
	}
	return /* @__PURE__ */ jsx(Card, {
		className: "border-navy/30",
		children: /* @__PURE__ */ jsxs(CardContent, {
			className: "space-y-4 pt-5",
			children: [
				/* @__PURE__ */ jsxs("div", {
					className: "flex flex-wrap items-start justify-between gap-3",
					children: [/* @__PURE__ */ jsxs("div", {
						className: "flex items-start gap-3",
						children: [v.photoUrl ? /* @__PURE__ */ jsx("img", {
							src: v.photoUrl,
							alt: "",
							className: "h-24 w-20 rounded-md object-cover"
						}) : /* @__PURE__ */ jsx("div", {
							className: "flex h-24 w-20 items-center justify-center rounded-md bg-navy text-paper",
							children: v.fullName.slice(0, 2).toUpperCase()
						}), /* @__PURE__ */ jsxs("div", { children: [
							/* @__PURE__ */ jsx("p", {
								className: "text-xs tracking-[0.16em] text-muted-foreground",
								children: "SMART RECORD"
							}),
							/* @__PURE__ */ jsx("h3", {
								className: "font-display text-xl font-semibold",
								children: v.fullName
							}),
							/* @__PURE__ */ jsxs("p", {
								className: "text-sm text-muted-foreground",
								children: [
									v.volunteerId,
									" · ",
									v.enrollment,
									" · ",
									v.unit,
									" · ",
									v.nssRole
								]
							}),
							rec.isBest ? /* @__PURE__ */ jsx(Badge, {
								tone: "saffron",
								className: "mt-1",
								children: "Best volunteer"
							}) : null
						] })]
					}), /* @__PURE__ */ jsxs("div", {
						className: "flex gap-2",
						children: [
							/* @__PURE__ */ jsx(Button, {
								size: "sm",
								variant: "outline",
								onClick: () => void openIdCard(v, state.settings),
								children: "ID card"
							}),
							/* @__PURE__ */ jsxs(Button, {
								size: "sm",
								variant: "outline",
								onClick: () => void showVolunteerQr(),
								children: [/* @__PURE__ */ jsx(QrCode, {}), "QR"]
							}),
							/* @__PURE__ */ jsx(Button, {
								size: "sm",
								variant: "ghost",
								onClick: onClose,
								children: "Close"
							})
						]
					})]
				}),
				qrOpen && qrMarkup ? /* @__PURE__ */ jsxs("div", {
					className: "rounded-xl border border-border bg-muted/30 p-4",
					children: [/* @__PURE__ */ jsxs("div", {
						className: "flex flex-wrap items-center justify-between gap-2",
						children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("p", {
							className: "text-sm font-medium",
							children: "Volunteer Identity QR"
						}), /* @__PURE__ */ jsx("p", {
							className: "text-xs text-muted-foreground",
							children: "Scan this QR after the Event QR to identify this volunteer."
						})] }), /* @__PURE__ */ jsxs("div", {
							className: "flex gap-2",
							children: [/* @__PURE__ */ jsx(Button, {
								size: "sm",
								variant: "outline",
								onClick: printVolunteerQr,
								children: "Print QR"
							}), /* @__PURE__ */ jsx(Button, {
								size: "sm",
								variant: "ghost",
								onClick: () => setQrOpen(false),
								children: "Close"
							})]
						})]
					}), /* @__PURE__ */ jsx("div", {
						className: "mx-auto mt-3 w-fit rounded-xl border border-border bg-white p-3",
						dangerouslySetInnerHTML: { __html: qrMarkup }
					})]
				}) : null,
				/* @__PURE__ */ jsxs("div", {
					className: "rounded-xl border border-border bg-muted/30 p-3",
					children: [/* @__PURE__ */ jsx("p", {
						className: "text-sm font-medium",
						children: "Achievement badges"
					}), /* @__PURE__ */ jsxs("div", {
						className: "mt-2 flex flex-wrap gap-2",
						children: [(v.badges ?? []).map((badge) => /* @__PURE__ */ jsxs("button", {
							type: "button",
							className: "rounded-full bg-saffron/15 px-3 py-1 text-xs",
							onClick: () => state.removeBadge(v.id, badge),
							children: [badge, " ×"]
						}, badge)), [
							"Eco Warrior",
							"Blood Donation Champion",
							"Best Volunteer",
							"Event Leader",
							"Community Service",
							"Attendance Champion",
							"Special Camp Participant"
						].filter((b) => !(v.badges ?? []).includes(b)).map((badge) => /* @__PURE__ */ jsxs(Button, {
							size: "sm",
							variant: "outline",
							onClick: () => state.awardBadge(v.id, badge),
							children: ["+ ", badge]
						}, badge))]
					})]
				}),
				/* @__PURE__ */ jsxs("div", {
					className: "grid grid-cols-2 gap-2 sm:grid-cols-4",
					children: [
						/* @__PURE__ */ jsx(Mini, {
							label: "Attendance",
							value: `${rec.pct}%`
						}),
						/* @__PURE__ */ jsx(Mini, {
							label: "Present",
							value: `${rec.present}/${rec.marked}`
						}),
						/* @__PURE__ */ jsx(Mini, {
							label: "Hours",
							value: String(rec.hours)
						}),
						/* @__PURE__ */ jsx(Mini, {
							label: "Certificates",
							value: String(rec.certs.length)
						})
					]
				}),
				/* @__PURE__ */ jsxs("dl", {
					className: "grid gap-2 text-sm sm:grid-cols-2",
					children: [
						/* @__PURE__ */ jsx(Row, {
							k: "Mobile",
							val: v.mobile
						}),
						/* @__PURE__ */ jsx(Row, {
							k: "Email",
							val: v.email || "—"
						}),
						/* @__PURE__ */ jsx(Row, {
							k: "Course",
							val: `${v.course} · Sem ${v.semester}`
						}),
						/* @__PURE__ */ jsx(Row, {
							k: "Gender / Blood",
							val: `${v.gender || "—"} · ${v.bloodGroup || "—"}`
						}),
						/* @__PURE__ */ jsx(Row, {
							k: "Emergency",
							val: v.emergencyContact || v.parentMobile || "—"
						}),
						/* @__PURE__ */ jsx(Row, {
							k: "Address",
							val: v.address || "—"
						})
					]
				}),
				/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("p", {
					className: "mb-2 text-sm font-medium",
					children: "Activity history"
				}), /* @__PURE__ */ jsx("div", {
					className: "max-h-56 overflow-auto rounded-md border border-border",
					children: /* @__PURE__ */ jsxs("table", {
						className: "w-full text-left text-xs",
						children: [/* @__PURE__ */ jsx("thead", {
							className: "bg-muted",
							children: /* @__PURE__ */ jsxs("tr", { children: [
								/* @__PURE__ */ jsx("th", {
									className: "px-2 py-1.5",
									children: "Event"
								}),
								/* @__PURE__ */ jsx("th", {
									className: "px-2 py-1.5",
									children: "Date"
								}),
								/* @__PURE__ */ jsx("th", {
									className: "px-2 py-1.5",
									children: "Status"
								}),
								/* @__PURE__ */ jsx("th", {
									className: "px-2 py-1.5",
									children: "Certificate"
								})
							] })
						}), /* @__PURE__ */ jsx("tbody", { children: events.map((e) => {
							const att = attendanceOf(state, v.id, e.id);
							const cert = rec.certs.find((c) => c.eventId === e.id);
							return /* @__PURE__ */ jsxs("tr", {
								className: "border-t border-border",
								children: [
									/* @__PURE__ */ jsx("td", {
										className: "px-2 py-1.5",
										children: e.name
									}),
									/* @__PURE__ */ jsx("td", {
										className: "px-2 py-1.5",
										children: formatLongDate(e.date)
									}),
									/* @__PURE__ */ jsx("td", {
										className: "px-2 py-1.5",
										children: att ? att.present ? "Present" : "Absent" : e.status === "upcoming" ? "Upcoming" : "—"
									}),
									/* @__PURE__ */ jsx("td", {
										className: "px-2 py-1.5",
										children: cert ? /* @__PURE__ */ jsx("button", {
											type: "button",
											className: "text-forest underline",
											onClick: () => void htmlForCertificate(cert, v, e, state.settings).then(openCertificateDocument),
											children: "View"
										}) : "—"
									})
								]
							}, e.id);
						}) })]
					})
				})] })
			]
		})
	});
}
function escapeHtml(value) {
	return value.replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll("\"", "&quot;").replaceAll("'", "&#039;");
}
function Mini({ label, value }) {
	return /* @__PURE__ */ jsxs("div", {
		className: "rounded-md border border-border bg-muted/40 px-3 py-2",
		children: [/* @__PURE__ */ jsx("p", {
			className: "font-display text-xl font-semibold tabular-nums",
			children: value
		}), /* @__PURE__ */ jsx("p", {
			className: "text-[11px] text-muted-foreground",
			children: label
		})]
	});
}
function Row({ k, val }) {
	return /* @__PURE__ */ jsxs("div", {
		className: "flex justify-between gap-3 border-b border-border/60 py-1",
		children: [/* @__PURE__ */ jsx("dt", {
			className: "text-muted-foreground",
			children: k
		}), /* @__PURE__ */ jsx("dd", {
			className: "text-right font-medium",
			children: val
		})]
	});
}
function EditVolunteer({ volunteer, onClose }) {
	const update = useNssStore((s) => s.updateVolunteer);
	const addLog = useNssStore((s) => s.addLog);
	const [form, setForm] = useState({
		fullName: volunteer.fullName,
		enrollment: volunteer.enrollment,
		mobile: volunteer.mobile,
		course: volunteer.course,
		semester: volunteer.semester,
		nssRole: volunteer.nssRole,
		photoUrl: volunteer.photoUrl ?? "",
		emergencyContact: volunteer.emergencyContact ?? ""
	});
	async function onPhoto(file) {
		if (!file) return;
		const { blob } = await compressPassport(file);
		const url = await blobToDataUrl(blob);
		setForm((f) => ({
			...f,
			photoUrl: url
		}));
	}
	function save(e) {
		e.preventDefault();
		update(volunteer.id, form);
		addLog("Updated Student", `Updated details for ${form.fullName}`);
		toast.success("Student details saved");
		onClose();
	}
	return /* @__PURE__ */ jsx(Card, { children: /* @__PURE__ */ jsxs(CardContent, {
		className: "space-y-3 pt-5",
		children: [/* @__PURE__ */ jsxs("p", {
			className: "font-medium",
			children: ["Edit ", volunteer.fullName]
		}), /* @__PURE__ */ jsxs("form", {
			className: "grid gap-3 sm:grid-cols-2",
			onSubmit: save,
			children: [
				/* @__PURE__ */ jsxs("div", {
					className: "sm:col-span-2 flex items-center gap-3",
					children: [form.photoUrl ? /* @__PURE__ */ jsx("img", {
						src: form.photoUrl,
						alt: "",
						className: "h-20 w-16 rounded object-cover"
					}) : null, /* @__PURE__ */ jsx("input", {
						type: "file",
						accept: "image/*",
						onChange: (e) => void onPhoto(e.target.files?.[0])
					})]
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "Full name",
					children: /* @__PURE__ */ jsx(Input, {
						value: form.fullName,
						onChange: (e) => setForm({
							...form,
							fullName: e.target.value
						})
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "Enrollment",
					children: /* @__PURE__ */ jsx(Input, {
						value: form.enrollment,
						onChange: (e) => setForm({
							...form,
							enrollment: e.target.value
						})
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "Mobile",
					children: /* @__PURE__ */ jsx(Input, {
						value: form.mobile,
						onChange: (e) => setForm({
							...form,
							mobile: e.target.value
						})
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "Course",
					children: /* @__PURE__ */ jsxs("select", {
						className: "h-10 w-full rounded-md border border-border bg-card px-3 text-sm",
						value: form.course,
						onChange: (e) => setForm({
							...form,
							course: e.target.value
						}),
						children: [
							/* @__PURE__ */ jsx("option", { children: "B.A." }),
							/* @__PURE__ */ jsx("option", { children: "B.Com." }),
							/* @__PURE__ */ jsx("option", { children: "B.Sc." })
						]
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "Semester",
					children: /* @__PURE__ */ jsx("select", {
						className: "h-10 w-full rounded-md border border-border bg-card px-3 text-sm",
						value: form.semester,
						onChange: (e) => setForm({
							...form,
							semester: e.target.value
						}),
						children: [
							"1",
							"2",
							"3",
							"4",
							"5",
							"6"
						].map((s) => /* @__PURE__ */ jsx("option", { children: s }, s))
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "Role",
					children: /* @__PURE__ */ jsxs("select", {
						className: "h-10 w-full rounded-md border border-border bg-card px-3 text-sm",
						value: form.nssRole,
						onChange: (e) => setForm({
							...form,
							nssRole: e.target.value
						}),
						children: [/* @__PURE__ */ jsx("option", { children: "Volunteer" }), /* @__PURE__ */ jsx("option", { children: "Leader" })]
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "Emergency contact",
					children: /* @__PURE__ */ jsx(Input, {
						value: form.emergencyContact,
						onChange: (e) => setForm({
							...form,
							emergencyContact: e.target.value
						})
					})
				}),
				/* @__PURE__ */ jsxs("div", {
					className: "sm:col-span-2 flex gap-2",
					children: [/* @__PURE__ */ jsx(Button, {
						type: "submit",
						children: "Save changes"
					}), /* @__PURE__ */ jsx(Button, {
						type: "button",
						variant: "outline",
						onClick: onClose,
						children: "Cancel"
					})]
				})
			]
		})]
	}) });
}
function Field({ label, children }) {
	return /* @__PURE__ */ jsxs("div", {
		className: "space-y-1.5",
		children: [/* @__PURE__ */ jsx(Label, { children: label }), children]
	});
}
//#endregion
export { VolunteersPage as component };
