import { O as firstNameOf, u as useNssStore } from "./utils-BIiJ-s-U.js";
import { t as Button } from "./button-HEpMk59d.js";
import { i as CardTitle, n as CardContent, r as CardHeader, t as Card } from "./card-2ADCK2Ma.js";
import { a as dashboardPath, c as loginStaff, d as loginWithMpin, f as lookupUsernameByMobile, h as useSessionStore, l as loginVolunteer, m as resetPasswordToMobile, r as Route, u as loginWithFingerprint } from "./router-Cdyjb-lJ.js";
import { t as AppShell } from "./shell-DsfHbM4s.js";
import { t as Input } from "./input-CRT4sBU_.js";
import { t as Label } from "./label-Bl0lQyM7.js";
import { useEffect, useState } from "react";
import { Link, useNavigate } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { Fingerprint, KeyRound } from "lucide-react";
import { toast } from "sonner";
//#region src/routes/login.tsx?tsr-split=component
function makeCaptcha() {
	const alphabet = "ABCDEFGHJKLMNPQRSTUVWXYZ23456789";
	let out = "";
	for (let i = 0; i < 5; i += 1) out += alphabet[Math.floor(Math.random() * 32)];
	return out;
}
function LoginPage() {
	const { role: roleHint } = Route.useSearch();
	const [role, setRole] = useState(roleHint ?? "volunteer");
	const [mode, setMode] = useState("password");
	const [id, setId] = useState("");
	const [password, setPassword] = useState("");
	const [captcha, setCaptcha] = useState(makeCaptcha);
	const [captchaIn, setCaptchaIn] = useState("");
	const [help, setHelp] = useState("none");
	const [helpMobile, setHelpMobile] = useState("");
	const [helpName, setHelpName] = useState("");
	const navigate = useNavigate();
	const settings = useNssStore((s) => s.settings);
	const volunteers = useNssStore((s) => s.volunteers);
	const updateVolunteer = useNssStore((s) => s.updateVolunteer);
	const setSession = useSessionStore((s) => s.setSession);
	useEffect(() => {
		if (roleHint) setRole(roleHint);
	}, [roleHint]);
	function refreshCaptcha() {
		setCaptcha(makeCaptcha());
		setCaptchaIn("");
	}
	function enter(session) {
		setSession(session);
		toast.success(`Welcome, ${session.name}`);
		navigate({ to: dashboardPath(session) });
	}
	function onSubmit(e) {
		e.preventDefault();
		if (captchaIn.trim().toUpperCase() !== captcha) {
			toast.error("Captcha does not match. Please type the letters again.");
			refreshCaptcha();
			return;
		}
		const result = role === "volunteer" ? mode === "mpin" ? loginWithMpin(id, password, volunteers) : loginVolunteer(id, password, volunteers) : loginStaff(role, id, password, settings);
		if (!result.ok) {
			toast.error(result.error);
			refreshCaptcha();
			return;
		}
		enter(result.session);
	}
	async function onFingerprint() {
		const result = await loginWithFingerprint(volunteers);
		if (!result.ok) {
			toast.error(result.error);
			return;
		}
		enter(result.session);
	}
	function recoverUsername() {
		const found = lookupUsernameByMobile(helpMobile, volunteers);
		if (!found) {
			toast.error("This mobile is not on the NSS roll.");
			return;
		}
		setId(found.firstName);
		toast.success(`Your username is ${found.firstName}`);
		setHelp("none");
	}
	function recoverPassword() {
		const v = resetPasswordToMobile(helpName, helpMobile, volunteers);
		if (!v) {
			toast.error("First name and registered mobile do not match.");
			return;
		}
		updateVolunteer(v.id, { loginPassword: "" });
		setId(firstNameOf(v.fullName));
		setPassword(v.mobile);
		toast.success("Password reset to your registered mobile number.");
		setHelp("none");
	}
	const staff = role !== "volunteer";
	return /* @__PURE__ */ jsx(AppShell, {
		eyebrow: "NSS PORTAL LOGIN",
		children: /* @__PURE__ */ jsx("div", {
			className: "mx-auto max-w-lg space-y-4 px-4 pb-16",
			children: /* @__PURE__ */ jsxs(Card, { children: [/* @__PURE__ */ jsx(CardHeader, { children: /* @__PURE__ */ jsx(CardTitle, { children: "Sign in" }) }), /* @__PURE__ */ jsxs(CardContent, { children: [
				/* @__PURE__ */ jsxs("form", {
					className: "space-y-4",
					onSubmit,
					children: [
						/* @__PURE__ */ jsxs("div", {
							className: "space-y-1.5",
							children: [/* @__PURE__ */ jsx(Label, {
								htmlFor: "role",
								children: "Login as"
							}), /* @__PURE__ */ jsxs("select", {
								id: "role",
								className: "flex h-10 w-full rounded-md border border-border bg-card px-3 text-sm",
								value: role,
								onChange: (e) => setRole(e.target.value),
								children: [
									/* @__PURE__ */ jsx("option", {
										value: "volunteer",
										children: "Student / Volunteer"
									}),
									/* @__PURE__ */ jsx("option", {
										value: "po",
										children: "Programme Officer"
									}),
									/* @__PURE__ */ jsx("option", {
										value: "admin",
										children: "Admin"
									})
								]
							})]
						}),
						/* @__PURE__ */ jsxs("div", {
							className: "space-y-1.5",
							children: [/* @__PURE__ */ jsx(Label, {
								htmlFor: "id",
								children: staff ? "Username" : "Username"
							}), /* @__PURE__ */ jsx(Input, {
								id: "id",
								value: id,
								onChange: (e) => setId(staff ? e.target.value : e.target.value.toUpperCase()),
								placeholder: staff ? "Username" : "Username",
								autoComplete: "username",
								required: true
							})]
						}),
						/* @__PURE__ */ jsxs("div", {
							className: "space-y-1.5",
							children: [/* @__PURE__ */ jsx(Label, {
								htmlFor: "pass",
								children: staff ? "Password" : mode === "mpin" ? "4-digit MPIN" : "Password"
							}), /* @__PURE__ */ jsx(Input, {
								id: "pass",
								type: mode === "mpin" ? "text" : "password",
								inputMode: mode === "mpin" || !staff ? "numeric" : "text",
								value: password,
								onChange: (e) => setPassword(e.target.value),
								placeholder: mode === "mpin" ? "MPIN" : "Password",
								autoComplete: "current-password",
								required: true
							})]
						}),
						/* @__PURE__ */ jsxs("div", {
							className: "space-y-1.5",
							children: [
								/* @__PURE__ */ jsx(Label, {
									htmlFor: "captcha",
									children: "Captcha"
								}),
								/* @__PURE__ */ jsxs("div", {
									className: "flex flex-wrap items-center gap-2",
									children: [/* @__PURE__ */ jsx("span", {
										"aria-hidden": true,
										className: "select-none rounded-md bg-navy px-3 py-2 font-mono text-lg tracking-[0.35em] text-paper",
										style: {
											fontStyle: "italic",
											letterSpacing: "0.28em"
										},
										children: captcha
									}), /* @__PURE__ */ jsx(Button, {
										type: "button",
										size: "sm",
										variant: "ghost",
										onClick: refreshCaptcha,
										children: "Refresh"
									})]
								}),
								/* @__PURE__ */ jsx(Input, {
									id: "captcha",
									value: captchaIn,
									onChange: (e) => setCaptchaIn(e.target.value.toUpperCase()),
									placeholder: "Captcha",
									autoComplete: "off",
									required: true
								})
							]
						}),
						/* @__PURE__ */ jsx(Button, {
							type: "submit",
							className: "w-full",
							size: "lg",
							children: "Sign in"
						})
					]
				}),
				!staff ? /* @__PURE__ */ jsxs("div", {
					className: "mt-3 flex flex-wrap gap-2",
					children: [
						/* @__PURE__ */ jsxs(Button, {
							type: "button",
							size: "sm",
							variant: mode === "mpin" ? "default" : "outline",
							onClick: () => {
								setMode(mode === "mpin" ? "password" : "mpin");
								setPassword("");
							},
							children: [/* @__PURE__ */ jsx(KeyRound, {}), mode === "mpin" ? "Use mobile password" : "Login with MPIN"]
						}),
						/* @__PURE__ */ jsxs(Button, {
							type: "button",
							size: "sm",
							variant: "outline",
							onClick: () => void onFingerprint(),
							children: [/* @__PURE__ */ jsx(Fingerprint, {}), "Login with fingerprint"]
						}),
						/* @__PURE__ */ jsx(Button, {
							type: "button",
							size: "sm",
							variant: "ghost",
							onClick: () => setHelp("user"),
							children: "Forgot username"
						}),
						/* @__PURE__ */ jsx(Button, {
							type: "button",
							size: "sm",
							variant: "ghost",
							onClick: () => setHelp("pass"),
							children: "Forgot password"
						})
					]
				}) : null,
				help === "user" ? /* @__PURE__ */ jsxs("div", {
					className: "mt-4 space-y-2 rounded-md border border-border p-3",
					children: [
						/* @__PURE__ */ jsx("p", {
							className: "text-sm font-medium",
							children: "Forgot username"
						}),
						/* @__PURE__ */ jsx(Input, {
							placeholder: "Registered mobile number",
							value: helpMobile,
							onChange: (e) => setHelpMobile(e.target.value)
						}),
						/* @__PURE__ */ jsxs("div", {
							className: "flex gap-2",
							children: [/* @__PURE__ */ jsx(Button, {
								type: "button",
								size: "sm",
								onClick: recoverUsername,
								children: "Show my username"
							}), /* @__PURE__ */ jsx(Button, {
								type: "button",
								size: "sm",
								variant: "ghost",
								onClick: () => setHelp("none"),
								children: "Close"
							})]
						})
					]
				}) : null,
				help === "pass" ? /* @__PURE__ */ jsxs("div", {
					className: "mt-4 space-y-2 rounded-md border border-border p-3",
					children: [
						/* @__PURE__ */ jsx("p", {
							className: "text-sm font-medium",
							children: "Forgot password — resets to registered mobile"
						}),
						/* @__PURE__ */ jsx(Input, {
							placeholder: "FIRST NAME in CAPITAL",
							value: helpName,
							onChange: (e) => setHelpName(e.target.value.toUpperCase())
						}),
						/* @__PURE__ */ jsx(Input, {
							placeholder: "Registered mobile number",
							value: helpMobile,
							onChange: (e) => setHelpMobile(e.target.value)
						}),
						/* @__PURE__ */ jsxs("div", {
							className: "flex gap-2",
							children: [/* @__PURE__ */ jsx(Button, {
								type: "button",
								size: "sm",
								onClick: recoverPassword,
								children: "Reset password"
							}), /* @__PURE__ */ jsx(Button, {
								type: "button",
								size: "sm",
								variant: "ghost",
								onClick: () => setHelp("none"),
								children: "Close"
							})]
						})
					]
				}) : null,
				/* @__PURE__ */ jsxs("p", {
					className: "mt-4 text-center text-sm text-muted-foreground",
					children: [
						"New volunteer?",
						" ",
						/* @__PURE__ */ jsx(Link, {
							to: "/register",
							className: "font-medium text-primary",
							children: "Register here"
						})
					]
				})
			] })] })
		})
	});
}
//#endregion
export { LoginPage as component };
