import { S as academicYear, k as formatLongDate, u as useNssStore } from "./utils-BIiJ-s-U.js";
import { t as Button } from "./button-HEpMk59d.js";
import { i as CardTitle, n as CardContent, r as CardHeader, t as Card } from "./card-2ADCK2Ma.js";
import { n as Route } from "./router-Cdyjb-lJ.js";
import { t as AppShell } from "./shell-DsfHbM4s.js";
import { t as Badge } from "./badge-BMh2Hxac.js";
import { t as certificateSerial } from "./certificates-BpqtJ9x9.js";
import { useMemo, useState } from "react";
import { jsx, jsxs } from "react/jsx-runtime";
import { CheckCircle2, Search, ShieldCheck, XCircle } from "lucide-react";
//#region src/routes/verify.tsx?tsr-split=component
function VerifyPage() {
	const { id: initialId } = Route.useSearch();
	const [id, setId] = useState(initialId);
	const state = useNssStore();
	const result = useMemo(() => {
		const query = id.trim().toLowerCase();
		if (!query) return null;
		const cert = (state.certificates ?? []).find((c) => {
			const volunteer = state.volunteers.find((v) => v.id === c.volunteerId);
			const event = state.events.find((e) => e.id === c.eventId);
			return Boolean(volunteer && event && (c.id.toLowerCase() === query || certificateSerial(volunteer, event).toLowerCase() === query));
		});
		if (!cert) return { found: false };
		return {
			found: true,
			cert,
			volunteer: state.volunteers.find((v) => v.id === cert.volunteerId),
			event: state.events.find((e) => e.id === cert.eventId)
		};
	}, [
		id,
		state.certificates,
		state.events,
		state.volunteers
	]);
	return /* @__PURE__ */ jsx(AppShell, {
		eyebrow: "PUBLIC CERTIFICATE VERIFICATION",
		current: "home",
		children: /* @__PURE__ */ jsxs("div", {
			className: "mx-auto max-w-3xl space-y-6 px-4 pb-16",
			children: [/* @__PURE__ */ jsxs(Card, {
				className: "overflow-hidden",
				children: [/* @__PURE__ */ jsxs(CardHeader, {
					className: "bg-navy text-paper",
					children: [/* @__PURE__ */ jsxs(CardTitle, {
						className: "flex items-center gap-2",
						children: [/* @__PURE__ */ jsx(ShieldCheck, {}), " Certificate Verification"]
					}), /* @__PURE__ */ jsx("p", {
						className: "text-sm text-paper/75",
						children: "Enter the Certificate ID printed on the certificate."
					})]
				}), /* @__PURE__ */ jsx(CardContent, {
					className: "space-y-3 pt-5",
					children: /* @__PURE__ */ jsxs("div", {
						className: "flex flex-col gap-2 sm:flex-row",
						children: [/* @__PURE__ */ jsx("input", {
							className: "h-11 flex-1 rounded-md border border-border bg-card px-3 text-sm",
							value: id,
							onChange: (e) => setId(e.target.value),
							placeholder: "NSS/202627/NSS001/..."
						}), /* @__PURE__ */ jsxs(Button, {
							onClick: () => setId((v) => v.trim()),
							children: [/* @__PURE__ */ jsx(Search, {}), " Verify"]
						})]
					})
				})]
			}), result?.found ? /* @__PURE__ */ jsx(Card, {
				className: "border-forest/40",
				children: /* @__PURE__ */ jsxs(CardContent, {
					className: "space-y-4 pt-6",
					children: [
						/* @__PURE__ */ jsxs("div", {
							className: "flex items-center gap-2 text-forest",
							children: [
								/* @__PURE__ */ jsx(CheckCircle2, {}),
								/* @__PURE__ */ jsx("strong", { children: "Certificate Verified" }),
								/* @__PURE__ */ jsx(Badge, {
									tone: "forest",
									children: "VALID"
								})
							]
						}),
						/* @__PURE__ */ jsxs("div", {
							className: "grid gap-3 sm:grid-cols-2 text-sm",
							children: [
								/* @__PURE__ */ jsx(Info, {
									label: "Student",
									value: result.volunteer.fullName
								}),
								/* @__PURE__ */ jsx(Info, {
									label: "Event",
									value: result.event.name
								}),
								/* @__PURE__ */ jsx(Info, {
									label: "Date",
									value: formatLongDate(result.event.date)
								}),
								/* @__PURE__ */ jsx(Info, {
									label: "Certificate ID",
									value: certificateSerial(result.volunteer, result.event)
								}),
								/* @__PURE__ */ jsx(Info, {
									label: "College",
									value: state.settings.collegeName
								}),
								/* @__PURE__ */ jsx(Info, {
									label: "Academic Year",
									value: academicYear(result.event.date)
								})
							]
						}),
						/* @__PURE__ */ jsx("p", {
							className: "text-xs text-muted-foreground",
							children: "Only certificate verification details are shown. Private student information is not exposed."
						})
					]
				})
			}) : result?.found === false ? /* @__PURE__ */ jsx(Card, {
				className: "border-danger/40",
				children: /* @__PURE__ */ jsxs(CardContent, {
					className: "flex items-center gap-2 pt-6 text-danger",
					children: [/* @__PURE__ */ jsx(XCircle, {}), /* @__PURE__ */ jsx("strong", { children: "Certificate not found or invalid." })]
				})
			}) : null]
		})
	});
}
function Info({ label, value }) {
	return /* @__PURE__ */ jsxs("div", {
		className: "rounded-xl border border-border p-3",
		children: [/* @__PURE__ */ jsx("p", {
			className: "text-xs text-muted-foreground",
			children: label
		}), /* @__PURE__ */ jsx("p", {
			className: "mt-1 font-medium",
			children: value
		})]
	});
}
//#endregion
export { VerifyPage as component };
